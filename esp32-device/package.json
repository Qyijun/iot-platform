{
  "name": "esp32-device",
  "version": "1.0.0",
  "description": "ESP32 device firmware",
  "scripts": {
    "build": "pio run",
    "upload": "pio run --target upload"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
